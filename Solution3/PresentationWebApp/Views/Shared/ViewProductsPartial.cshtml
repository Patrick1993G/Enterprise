@model IEnumerable<ShoppingCart.Application.ViewModels.ProductViewModel>

@foreach (var p in Model)
{
    @if ((!@p.Disable && !User.IsInRole("Admin")) || User.IsInRole("Admin"))
    {
        <div class="col-4">
            <div class="card" style="width: 18rem;">
                <img src="@p.ImageUrl" class="card-img-top image" alt="...">
                <div class="card-body">
                    <h5 class="card-title">@p.Name</h5>
                    <p class="card-text">€ @p.Price</p>
                    <a href="/Products/Details?id=@p.Id" class="btn btn-primary">View Specifications</a>
                    @if (User.IsInRole("Admin"))
                    {
                        <a href="/Products/Delete?id=@p.Id" class="btn btn-danger">Delete</a>
                        @if (@p.Disable)
                        {
                            <a href="/Products/Disable?id=@p.Id" class="btn btn-secondary">Enable</a>
                        }
                        else
                        {
                            <a href="/Products/Disable?id=@p.Id" class="btn btn-secondary">Disable</a>
                        }

                    }
                </div>
            </div>
        </div>
    }

}